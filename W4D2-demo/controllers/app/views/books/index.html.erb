<h1>All the Books!</h1>

<ul>
  <% @books.each do |book| %>
    <li><%= book.title %> by <%= book.author %></li>
    <%= button_to "delete", book_url(book), method: :delete %>
    <br>
    <!--#TODO Close out your tags -->
  <% end %>
</ul>

<%= link_to "Add a book", "/books/new" %>
